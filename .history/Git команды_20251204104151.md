## 🚀 Начало работы

| Команда | Описание | Пример |
|---------|----------|--------|
| `git init` | Инициализация нового репозитория | `git init` |
| `git clone` | Клонирование удаленного репозитория | `git clone https://github.com/user/repo.git` |

## 📁 Работа с файлами

| Команда | Описание | Пример |
|---------|----------|--------|
| `git status` | Показать состояние файлов | `git status` |
| `git add` | Добавить файлы в staging | `git add file.txt` |
| `git commit` | Создать коммит | `git commit -m "Сообщение"` |

## 🌿 Ветвление

| Команда | Описание | Пример |
|---------|----------|--------|
| `git branch` | Показать ветки | `git branch` |
| `git checkout` | Переключиться на ветку | `git checkout feature` |
| `git merge` | Слить ветки | `git merge feature` |

## 🔄 Работа с удаленными репозиториями

| Команда | Описание | Пример |
|---------|----------|--------|
| `git remote` | Управление удаленными репозиториями | `git remote add origin URL` |
| `git push` | Отправить изменения | `git push origin main` |
| `git pull` | Получить изменения | `git pull origin main` |

## 📜 Просмотр истории

| Команда | Описание | Пример |
|---------|----------|--------|
| `git log` | Показать историю коммитов | `git log --oneline` |
| `git diff` | Показать изменения | `git diff` |

## ↩️ Отмена изменений

| Команда | Описание | Пример |
|---------|----------|--------|
| `git reset` | Отменить коммит | `git reset HEAD~1` |
| `git revert` | Создать отменяющий коммит | `git revert COMMIT_ID` |
| `git checkout --` | Отменить изменения в файле | `git checkout -- file.txt` |
🔧 Шаг 3: Добавляем команды из своей таблицы
Если у тебя уже есть своя таблица в другом формате, можешь добавить их:

markdown
## 🎯 Мои часто используемые команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `git config` | Настройка Git | `git config --global user.name "Твое имя"` |
| `git stash` | Временно сохранить изменения | `git stash` |
| `git tag` | Работа с тегами | `git tag v1.0.0` |
💡 Шаг 4: Делаем как у sakuracreator — добавляем эмодзи и стиль
markdown
# 🐙 Полная таблица Git команд
*Все команды Git в одном месте для быстрого доступа*

## 📑 Содержание
- [Начало работы](#-начало-работы)
- [Работа с файлами](#-работа-с-файлами)
- [Ветвление](#-ветвление)
- [Удаленные репозитории](#-работа-с-удаленными-репозиториями)
- [История](#-просмотр-истории)
- [Отмена изменений](#-отмена-изменений)

## 🚀 Начало работы

> 💡 **Совет:** Начни с `git init` для нового проекта или `git clone` для существующего

| Команда | Описание | Пример |
|---------|----------|--------|
| `git init` | 🆕 Создать новый репозиторий | `git init` |
| `git clone` | 📥 Скопировать репозиторий | `git clone https://github.com/user/repo.git` |
| `git config` | ⚙️ Настройка пользователя | `git config --global user.name "Имя"` |

## 📁 Работа с файлами

> ⚠️ **Важно:** Всегда проверяй `git status` перед коммитом!

| Команда | Описание
не там в ридми"Git команды.md" вот это новая
А, понял! Ты создал файл с именем Git команды.md (с пробелом и на русском). Отлично! Теперь давай его заполним.

📝 Шаг 1: Открываем файл в VS Code
powershell
# Открываем файл (если название с пробелом - в кавычках)
code "Git команды.md"

# Или через интерфейс VS Code:
# 1. Открой папку проекта
# 2. В панели слева найди файл "Git команды.md"
# 3. Кликни на него два раза
🎨 Шаг 2: Копируем структуру из того файла
Вставь в файл этот шаблон (или скопируй из того файла на GitHub):

markdown
# Полная таблица Git команд

## 📌 Настройка Git

| Команда | Описание | Пример |
|---------|----------|--------|
| `git config --global user.name` | Установить имя пользователя | `git config --global user.name "Ваше Имя"` |
| `git config --global user.email` | Установить email пользователя | `git config --global user.email "ваш@email.com"` |
| `git config --list` | Показать все настройки | `git config --list` |

## 🚀 Создание репозитория

| Команда | Описание | Пример |
|---------|----------|--------|
| `git init` | Инициализировать новый репозиторий | `git init` |
| `git clone` | Клонировать существующий репозиторий | `git clone https://github.com/user/repo.git` |

## 📁 Внесение изменений

| Команда | Описание | Пример |
|---------|----------|--------|
| `git status` | Показать состояние рабочего каталога | `git status` |
| `git add` | Добавить файлы в индекс | `git add file.txt` или `git add .` |
| `git commit` | Зафиксировать изменения | `git commit -m "Сообщение коммита"` |
| `git rm` | Удалить файлы из индекса и рабочего каталога | `git rm file.txt` |

## 🌿 Ветвление

| Команда | Описание | Пример |
|---------|----------|--------|
| `git branch` | Показать список веток | `git branch` |
| `git branch <name>` | Создать новую ветку | `git branch feature` |
| `git checkout <branch>` | Переключиться на ветку | `git checkout main` |
| `git checkout -b <branch>` | Создать и переключиться на ветку | `git checkout -b new-feature` |
| `git merge <branch>` | Слить ветку с текущей | `git merge feature` |

## 📜 История

| Команда | Описание | Пример |
|---------|----------|--------|
| `git log` | Показать историю коммитов | `git log` |
| `git log --oneline` | Краткая история коммитов | `git log --oneline` |
| `git log --graph` | История с визуализацией ветвления | `git log --graph --oneline --all` |
| `git diff` | Показать изменения между коммитами | `git diff HEAD~1 HEAD` |

## ↩️ Отмена изменений

| Команда | Описание | Пример |
|---------|----------|--------|
| `git reset` | Отменить индексацию файла | `git reset file.txt` |
| `git reset --hard` | Отменить все незакоммиченные изменения | `git reset --hard HEAD` |
| `git revert` | Создать новый коммит, отменяющий изменения | `git revert <commit>` |
| `git checkout -- <file>` | Отменить изменения в файле | `git checkout -- file.txt` |

## 🌐 Удаленные репозитории

| Команда | Описание | Пример |
|---------|----------|--------|
| `git remote` | Показать список удаленных репозиториев | `git remote -v` |
| `git remote add` | Добавить удаленный репозиторий | `git remote add origin https://github.com/user/repo.git` |
| `git push` | Отправить изменения в удаленный репозиторий | `git push origin main` |
| `git pull` | Получить изменения из удаленного репозитория | `git pull origin main` |
| `git fetch` | Загрузить изменения без слияния | `git fetch origin` |