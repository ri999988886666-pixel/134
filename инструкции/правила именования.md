# 🎯 Правила именования для новичков: просто и понятно

## 📌 **Самое главное правило**
> **Имя должно говорить, ЗАЧЕМ существует эта переменная/функция/класс, а не КАКОГО ОНА ТИПА**

---

## 🎨 **1. Три основных стиля (запомни как алфавит)**

### **a) Верблюжий стиль (camelCase)**
- **Для чего:** переменные, функции, методы
- **Как:** первое слово с маленькой буквы, остальные с большой
```
firstName, getUserData, calculateTotalPrice, isActive
```

### **b) Паскаль стиль (PascalCase)**
- **Для чего:** классы, интерфейсы, компоненты React
- **Как:** каждое слово с большой буквы
```
UserProfile, DatabaseConnection, ShoppingCart
```

### **c) Змеиный стиль (snake_case)**
- **Для чего:** константы, базы данных, Python-переменные
- **Как:** всё маленькими буквами, слова через _
```
MAX_SIZE, user_name, created_at
```

---

## 🧩 **2. Что как называть (шпаргалка)**

### **✅ ПЕРЕМЕННЫЕ (существительные)**
```javascript
// ХОРОШО - понятно что хранится
let userName = "Анна";
let itemPrice = 1000;
let isLoading = true;

// ПЛОХО - непонятно
let a = "Анна";       // Что за "a"?
let price1 = 1000;    // А что такое price2?
let flag = true;      // Какой флаг? Зачем?
```

### **✅ ФУНКЦИИ (глаголы - что делает?)**
```javascript
// ХОРОШО - понятно что делает
function calculateSum(a, b) { return a + b; }
function getUserById(id) { /* ... */ }
function isValidEmail(email) { /* ... */ }

// ПЛОХО - непонятно
function proc() { /* ... */ }          // Что обрабатывает?
function check() { /* ... */ }         // Что проверяет?
function doSomething() { /* ... */ }   // Что именно делает?
```

### **✅ КЛАССЫ (существительные с большой буквы)**
```javascript
// ХОРОШО
class User {}
class CarEngine {}
class PaymentProcessor {}

// ПЛОХО
class user {}          // должно быть User
class car_engine {}    // стиль не тот
class doPayment {}     // класс - не функция
```

### **✅ БУЛЕВЫЕ ПЕРЕМЕННЫЕ (вопросы)**
```javascript
// Хорошие префиксы:
let isActive = true;      // Активен ли?
let hasPermission = false; // Есть ли разрешение?
let canEdit = true;       // Может редактировать?
let shouldUpdate = false; // Нужно обновлять?

// Плохо:
let active = true;        // Активен? Или действие?
let permission = false;   // Это само разрешение или факт его наличия?
```

---

## 🚫 **3. Чего НЕ делать (запомни эти ошибки!)**

### **❌ Слишком короткие имена**
```javascript
// ПЛОХО (кроме счетчиков циклов)
let d;          // что за d? date? data? document?
let fn;         // function? first name?
let usr;        // user? а почему не полностью?
let tmp;        // temporary? а что временное?

// ХОРОШО (только для мелких областей видимости)
for (let i = 0; i < 10; i++) {}  // i,j,k - только в циклах!
```

### **❌ Слишком длинные имена**
```javascript
// ПЛОХО - слишком много слов
let numberOfFailedAttemptsToConnectToDatabase;

// ХОРОШО - оптимально
let connectionAttempts;
let failedConnectionCount;
```

### **❌ Неинформативные имена**
```javascript
// ПЛОХО - что это? зачем?
let data;       // Какие данные? Откуда?
let info;       // Какая информация?
let stuff;      // Вещи? Что за вещи?
let things;     // Прямо "штуки"?
let object;     // Какой объект?

// ХОРОШО - конкретно
let userData;
let productInfo;
let configObject;
```

### **❌ Разные написания одного и того же**
```javascript
// ПЛОХО - везде по-разному
getUser()
fetchUserData()
retrieveUserInfo()

// ХОРОШО - единообразие
getUser()       // везде getUser
getProduct()
getOrder()
```

---

## 📁 **4. Имена файлов и папок**

### **Файлы JavaScript/TypeScript**
```
// ХОРОШО
UserProfile.jsx     // Компонент React
userService.js      // Сервис
constants.js        // Константы
utils.js           // Вспомогательные функции

// ПЛОХО
userprofile.js      // без заглавных
user_profile.js     // смешиваем стили
file1.js           // непонятно
```

### **Папки**
```
src/
├── components/     // мн. число
├── utils/         // сокращение от utilities
├── constants/     // что хранится
├── api/           // работа с API
└── styles/        // стили
```

---

## 🎯 **5. Простой алгоритм для новичка**

### **Шаг 1: Спроси себя**
- **Для переменной:** "Что здесь хранится?"
  - Ответ: "Возраст пользователя" → `userAge`
  
- **Для функции:** "Что она делает?"
  - Ответ: "Проверяет email" → `validateEmail` или `isValidEmail`
  
- **Для класса:** "Что это за объект?"
  - Ответ: "Менеджер корзины покупок" → `ShoppingCartManager`

### **Шаг 2: Проверь по чек-листу**
- [ ] Имя понятно без комментариев?
- [ ] Не слишком длинное (1-3 слова)?
- [ ] Правильный стиль (camelCase/PascalCase)?
- [ ] Нет аббревиатур (кроме id, api, url)?

### **Шаг 3: Если сомневаешься**
```javascript
// Вместо этого:
let d = 10;  // дни? доллары? дистанция?

// Спроси себя: "Что это?"
// Это "количество дней отпуска"

// Пиши так:
let vacationDays = 10;
```

---

## 🏆 **6. Примеры "до" и "после"**

### **Пример 1: Плохо → Хорошо**
```javascript
// БЫЛО (плохо)
function p(a, b) {
    let r = a + b;
    return r;
}

// СТАЛО (хорошо)
function calculateSum(number1, number2) {
    let result = number1 + number2;
    return result;
}

// ЕЩЁ ЛУЧШЕ (еще короче и понятнее)
function sum(a, b) {
    return a + b;
}
```

### **Пример 2: Плохо → Хорошо**
```javascript
// БЫЛО (плохо)
let x = document.getElementById('btn');
let y = false;
let z = [];

// СТАЛО (хорошо)
let submitButton = document.getElementById('submit-btn');
let isFormValid = false;
let userList = [];
```

---

## 📚 **7. Быстрые советы на каждый день**

1. **Переменные** — существительные (`user`, `product`, `price`)
2. **Функции** — глаголы (`get`, `calculate`, `validate`, `send`)
3. **Классы** — существительные с большой буквы (`User`, `Car`, `Database`)
4. **Константы** — большими буквами (`MAX_SIZE`, `API_URL`)
5. **Булевы** — начинай с `is`, `has`, `can` (`isActive`, `hasError`)

---

## 🎮 **Тренировка (проверь себя)**

**Задание:** Переименуй плохие имена в хорошие:
```javascript
// 1. Что хранится? "Название товара"
let n;

// 2. Что делает функция? "Проверяет пароль"
function chk(p) { }

// 3. Что за класс? "Обработчик платежей"
class proc { }

// Ответы:
// 1. let productName;
// 2. function validatePassword(password) { }
// 3. class PaymentProcessor { }
```

---

## 💎 **Золотое правило для новичка**

> **Пиши код так, как будто его будет читать человек, который знает только русский/английский язык, но не знает программирования.**

Если твоя мама/друг/коллега с другого отдела посмотрит на имя и поймет, что оно значит — ты на правильном пути! 🚀

**Начни с этих простых правил, а тонкости (типы данных в именах, паттерны) придут с опытом. Главное — начать писать понятные имена уже сегодня!**