### Что такое GitHub?
Это платформа для хранения кода и совместной работы над проектами с помощью системы контроля версий **Git**. Представьте его как «Google Документы» для программистов, но с гораздо большими возможностями.

### Ключевые понятия:
*   **Репозиторий (репа)** — папка проекта, где хранятся все его файлы и история изменений.
*   **Форк (fork)** — ваша **личная копия** чужого репозитория. Вы можете делать с ней что угодно, не влияя на оригинал.
*   **Клонирование (clone)** — скачивание репозитория с GitHub на ваш компьютер.
*   **Коммит (commit)** — сохранение изменений в локальной истории Git с комментарием.
*   **Ветка (branch)** — изолированная линия разработки. По умолчанию все работают в ветке `main` (или `master`).
*   **Пул-реквест (PR) или Мёрж-реквест (MR)** — запрос на внесение ваших изменений из форка (или ветки) обратно в оригинальный проект.

---

### 🚀 Пошаговая инструкция: как внести свой вклад в чужой проект

#### Шаг 1: Сделайте форк
1.  Найдите интересный проект на GitHub (например, `github.com/имя/репозиторий`).
2.  В правом верхнем углу нажмите кнопку **Fork**. У вас появится точная копия этого репозитория под вашим аккаунтом (`github.com/ваше-имя/репозиторий`).

> **Важно:** Теперь вы работаете со своей копией.

#### Шаг 2: Склонируйте форк на компьютер
1.  На странице **вашего форка** нажмите зелёную кнопку **Code**.
2.  Скопируйте ссылку (SSH или HTTPS).
3.  Откройте терминал (командную строку) на компьютере и выполните:
    ```bash
    git clone [ваша-скопированная-ссылка]
    ```
    Пример:
    ```bash
    git clone https://github.com/ваше-имя/репозиторий.git
    ```
4.  Перейдите в папку проекта:
    ```bash
    cd репозиторий
    ```

#### Шаг 3: Настройте связь с оригиналом (upstream)
1.  Это нужно, чтобы следить за изменениями в исходном проекте.
2.  Перейдите на страницу **оригинального репозитория** (не вашего форка!) и скопируйте его ссылку.
3.  В терминале выполните:
    ```bash
    git remote add upstream [ссылка-на-оригинальный-репозиторий]
    ```
    Пример:
    ```bash
    git remote add upstream https://github.com/автор/репозиторий.git
    ```
4.  Проверьте, что всё настроено:
    ```bash
    git remote -v
    ```
    Вы должны увидеть:
    *   `origin` — ссылка на ваш форк (можно пушить).
    *   `upstream` — ссылка на оригинал (только для подтягивания изменений).

#### Шаг 4: Создайте ветку для своей задачи
**Никогда не работайте в ветке `main`!** Создайте новую ветку для каждой задачи.
```bash
git checkout -b feature/my-new-feature
```
*   `feature/my-new-feature` — название ветки. Оно должно быть понятным (например, `fix/typo-in-readme`).

#### Шаг 5: Внесите изменения и сохраните
1.  Откройте файлы проекта в редакторе кода (VS Code, Sublime и т.д.).
2.  Внесите нужные правки.
3.  Сохраните изменения (коммит) локально:
    ```bash
    # Добавьте изменённые файлы в «корзину» для коммита
    git add .
    # Или git add имя_файла.js (чтобы добавить конкретный файл)
    
    # Создайте коммит с понятным сообщением
    git commit -m "Добавил новую функцию: ..."
    ```

#### Шаг 6: Загрузите изменения в ваш форк на GitHub
```bash
git push origin feature/my-new-feature
```

#### Шаг 7: Создайте Pull Request (PR)
1.  Зайдите на страницу **вашего форка** на GitHub.
2.  Появится жёлтая плашка с кнопкой **Compare & pull request**. Нажмите её.
3.  Заполните форму:
    *   **Title** — краткое описание изменений.
    *   **Description** — детальное описание *что* и *почему* вы изменили.
4.  Нажмите **Create pull request**. Теперь автор оригинального проекта увидит ваши изменения и сможет принять их, отклонить или попросить доработать.

---

### 🔄 Как поддерживать форк в актуальном состоянии
Чтобы ваш форк не отставал от оригинала, периодически выполняйте:

```bash
# Переключитесь на свою основную ветку
git checkout main

# «Стяните» все изменения из оригинального репозитория
git fetch upstream

# Слейте их со своей веткой main
git merge upstream/main

# Загрузите обновлённый main в ваш форк на GitHub
git push origin main
```

### 🛠 Краткий чек-лист для новичка:
1.  **Fork** → Сделайте свою копию проекта.
2.  **Clone** → Скачайте её на компьютер (`git clone`).
3.  **Branch** → Создайте новую ветку для задачи (`git checkout -b branch-name`).
4.  **Code** → Внесите изменения.
5.  **Commit** → Сохраните их локально (`git add .` и `git commit -m "message"`).
6.  **Push** → Загрузите ветку в свой форк (`git push origin branch-name`).
7.  **PR** → Создайте Pull Request на GitHub.
8.  **Sync** → Регулярно обновляйте свой форк через `upstream`.

### 💡 Советы:
*   Перед началом работы проверьте `README.md` и `CONTRIBUTING.md` (если есть) в оригинальном репозитории — там могут быть правила для участников.
*   Пишите **понятные сообщения коммитов** на английском или языке проекта.
*   **Не бойтесь!** Даже если сделаете ошибку, Git позволяет всё исправить. Это лучший способ научиться.

Удачи в первом вкладе